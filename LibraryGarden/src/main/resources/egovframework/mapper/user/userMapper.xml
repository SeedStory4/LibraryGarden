<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="libraryGarden.user.mapper.UserMapper">

    <!-- 회원 가입 -->
    <insert id="insertUser" parameterType="libraryGarden.domain.UserVo">
        INSERT INTO USER (
            id, password, name, phone, email, address, userNumber
        ) VALUES (
            #{id}, #{password}, #{name}, #{phone}, #{email}, #{address}, #{userNumber}
        )
    </insert>

    <!-- 아이디 중복 확인 -->
    <select id="countUserById" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM USER WHERE id = #{id}
    </select>
    
    <!-- 마지막 userNumber 조회 -->
	<select id="getLastUserNumber" resultType="String">
	    SELECT MAX(userNumber) FROM USER
	</select>
	
	<select id="login" parameterType="string" resultType="libraryGarden.domain.UserVo">
	    SELECT *
	    FROM USER
	    WHERE ID = #{id}
	</select>

	
	<select id="selectUserById" resultType="libraryGarden.domain.UserVo" parameterType="String">
	  SELECT * FROM USER WHERE ID = #{id}
	</select>
	
	<update id="updateUser" parameterType="libraryGarden.domain.UserVo">
	    UPDATE USER
	    SET password = #{password},
	        phone = #{phone},
	        email = #{email},
	        address = #{address}
	    WHERE id = #{id}
	</update>
	
	<select id="selectIdByNameAndPhone" resultType="String">
	  SELECT id
	  FROM user
	  WHERE name = #{name}
	    AND phone = #{phone}
	</select>
	
	<update id="updatePasswordByPhone" parameterType="map">
	    UPDATE user
	    SET password = #{password}
	    WHERE id = #{id} AND phone = #{phone}
	</update>
	
	<select id="findEmailByIdAndPhone" resultType="String">
	    SELECT email FROM user WHERE id = #{id} AND phone = #{phone}
	</select>
	
	<select id="selectAllUsers" resultType="libraryGarden.domain.UserVo">
	    SELECT 
	        userNumber,
	        name,
	        id,
	        phone,
	        role,
	        DATE_FORMAT(date, '%Y.%m.%d') AS date
	    FROM user
	    ORDER BY userNumber DESC
	</select>
	
	
	<select id="searchUsersByCriteria" resultType="libraryGarden.domain.UserVo" parameterType="libraryGarden.domain.SearchCriteria">
	    SELECT 
	        userNumber,
	        name,
	        id,
	        phone,
	        role,
	        DATE_FORMAT(date, '%Y.%m.%d') AS date
	    FROM user
	    <where>
	        <if test="searchType != null and keyword != null and keyword != ''">
	            <choose>
	                <when test="searchType == 'name'">
	                    name LIKE CONCAT('%', #{keyword}, '%')
	                </when>
	                <when test="searchType == 'id'">
	                    id LIKE CONCAT('%', #{keyword}, '%')
	                </when>
	                <when test="searchType == 'role'">
	                    role LIKE CONCAT('%', #{keyword}, '%')
	                </when>
	            </choose>
	        </if>
	    </where>
	    ORDER BY date DESC
	</select>
	
		
	
	

</mapper>
