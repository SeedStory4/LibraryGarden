<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="libraryGarden.admin.mapper.AdminBookLoanMapper">

	<!-- 회원 이름 조회 -->
	<select id="selectUserName" parameterType="String"
		resultType="String">
		SELECT name
		FROM USER
		WHERE userNumber = #{userNumber}
	</select>

	<!-- 대출 가능 여부 조회 -->
	<select id="selectUserLoanStatus" parameterType="String"
		resultType="String">
		SELECT CASE
		WHEN EXISTS (
		SELECT 1
		FROM OVERDUE o
		JOIN USER u ON o.uidx = u.uidx
		WHERE u.userNumber = #{userNumber} AND o.status = 'Y'
		)
		THEN CONCAT('이용불가(~',
		(SELECT DATE_FORMAT(MAX(o2.endDate), '%Y-%m-%d')
		FROM OVERDUE o2
		JOIN USER u2 ON o2.uidx = u2.uidx
		WHERE u2.userNumber = #{userNumber} AND o2.status = 'Y'
		),
		')')
		ELSE '이용가능'
		END AS loanStatus
		FROM USER u
		WHERE u.userNumber = #{userNumber};
	</select>


	<!-- 대출 목록 조회 -->
	<select id="selectUserLoanList" parameterType="String"
		resultType="map">
		SELECT b.coverImg, b.title, b.author, lb.code, l.loanDate, l.dueDate,
		l.returnDate, l.status
		FROM LOAN l
		JOIN USER u ON l.uidx = u.uidx
		JOIN LIBRARY_BOOKS lb ON l.lbidx = lb.lbidx
		JOIN BOOKS b ON lb.bidx = b.bidx
		WHERE u.userNumber = #{userNumber}
		ORDER BY l.loanDate DESC;
	</select>

</mapper>